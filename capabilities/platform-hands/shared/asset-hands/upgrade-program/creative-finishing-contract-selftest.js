'use strict';
const assert=require('assert');const Hands=require('./creative-finishing-hands');const VE=require('./vector-engine');const AM=require('./precision-audio-micro');const R=require('./precision-raster');
const open=VE.createPath({id:'open',intentionally_open:true,commands:[{type:'M',to:{x:0,y:0}},{type:'L',to:{x:10,y:0}}]});assert.throws(()=>Hands.invoke('creative.vector-finish.resample',{path:open,spec:{count:100001}}),/resample count outside/);assert.throws(()=>Hands.invoke('creative.vector-finish.dash',{path:open,spec:{on:0,off:0}}),/dash pattern invalid/);const text=Hands.invoke('creative.typography-edit.create',{spec:{id:'t',text:'abc',style:{font_size:12,line_height:14}}}).result;assert.throws(()=>Hands.invoke('creative.typography-edit.insert',{document:text,index:9,text:'x'}),/insert index out of range/);assert.throws(()=>Hands.invoke('creative.typography-edit.fit',{document:text,spec:{width:0,height:20}}),/fit bounds invalid/);
const audio=AM.create({sample_rate:8000,channels:1,samples:new Array(50000).fill(.1)});assert.throws(()=>Hands.invoke('creative.audio-mastering.parametric-eq',{audio,spec:{frequency_hz:5000,q:1,gain_db:3}}),/parametric EQ bounds invalid/);assert.throws(()=>Hands.invoke('creative.audio-mastering.fir-convolution',{audio,spec:{kernel:new Array(1024).fill(1/1024)}}),/FIR work budget exceeded/);const longAudio=AM.create({sample_rate:8000,channels:1,samples:new Array(700000).fill(.1)});assert.throws(()=>Hands.invoke('creative.audio-mastering.moving-denoise',{audio:longAudio,spec:{radius:32,strength:.5}}),/moving-denoise work budget exceeded/);
function blank(w,h){return R.image({width:w,height:h,rgba:Buffer.alloc(w*h*4)});}const a=blank(256,256),b=blank(256,256);assert.throws(()=>Hands.invoke('creative.frame-finish.block-match-track',{reference:a,current:b,spec:{x:32,y:32,width:128,height:128,search_radius:64}}),/block-match work budget exceeded/);assert.throws(()=>Hands.invoke('creative.frame-finish.chroma-key',{image:a,spec:{tolerance:500,softness:0}}),/chroma key bounds invalid/);const track={schema:'axm.precision-frame-track/v1',current_digest:'wrong',dx:1,dy:1,digest:'wrong'};assert.throws(()=>Hands.invoke('creative.frame-finish.stabilize-translation',{image:a,track}),/track must bind exact current frame/);
console.log(JSON.stringify({status:'PASS',contract:'finishing-fail-closed',hands:Hands.audit().total},null,2));