{
  "capabilities": [
    { "id": "validation.reference.match-artifact-claim-canvas", "status": "available", "constraints": ["exact identifiers only; no assurance downgrade"] },
    { "id": "validation.reference.visible-missing-state", "status": "available", "constraints": ["MISSING_VALIDATOR remains distinct from artifact failure"] },
    { "id": "validation.reference.bind-artifact-digest", "status": "available", "constraints": ["portable digest plus fresh SHA-256"] },
    { "id": "validation.reference.bind-target-canvas-digest", "status": "available", "constraints": ["portable digest plus canonical SHA-256"] },
    { "id": "validation.reference.declare-assurance-scope", "status": "available", "constraints": ["structural, schema and official-checker scopes are distinct"] },
    { "id": "validation.musicxml.python-lxml-structure", "status": "available", "constraints": ["requires configured Python 3 with lxml"] },
    { "id": "validation.pdf.python-pypdf-structure", "status": "available", "constraints": ["requires configured Python 3 with pypdf", "does not certify PDF/X or PDF/UA"] },
    { "id": "validation.musicxml.w3c-xsd", "status": "available", "constraints": ["requires separately managed official MusicXML 4.0 XSD set"] },
    { "id": "validation.openusd.usdchecker", "status": "available", "constraints": ["requires separately managed official usd-core or usdchecker"] },
    { "id": "validation.epub.w3c-epubcheck", "status": "unavailable", "constraints": ["adapter exists; audited machine lacks Java and W3C EPUBCheck"] },
    { "id": "validation.reference.result-envelope", "status": "available", "constraints": ["binds immutable base result ID and digest"] },
    { "id": "validation.reference.backward-compatible-state", "status": "available", "constraints": ["reference_validation remains optional"] }
  ]
}
