{
  "id": "AXM-CAP-PHYSICS-CONSTRAINT-COMPOSER",
  "version": "0.2.0",
  "status": "candidate",
  "purpose": "Compose translation mounts and center-to-center distance joints around one shared donor-core integration step, with bounded convergence-aware early exit so already-satisfied constraint passes do not consume the full configured budget.",
  "handles": [
    "mixed-constraint-step",
    "single-integration-constraint-composition",
    "translation-mount-plus-distance-joint",
    "bounded-mixed-constraint-stabilization",
    "convergence-aware-constraint-pass"
  ],
  "input_contract": {
    "required": ["world", "constraints", "dt"],
    "properties": {
      "world": "axm.physics-world/v1 world",
      "constraints.mounts": "bounded translation-mount array",
      "constraints.distanceJoints": "bounded center-to-center distance-joint array",
      "dt": "one donor-core step delta",
      "options": "bounded family pass counts, bounded per-family position/velocity iteration counts, optional early-exit enablement, and explicit position/velocity convergence tolerances"
    }
  },
  "output_contract": {
    "kind": "axm.uc-constraint-composer-step/v0.1",
    "contains": [
      "final composed world",
      "normalized constraint definitions",
      "fixed family-order evidence",
      "pre/core/post constraint measurements",
      "position and constrained-relative-velocity residuals",
      "bounded pass summaries with convergence evidence",
      "configured versus executed pass counts and avoided-pass accounting",
      "preserved core-stage contact evidence",
      "final state checksum"
    ]
  },
  "dependencies": [
    "AXM-CAP-PHYSICS-FABRIC",
    "AXM-CAP-PHYSICS-DISTANCE-JOINTS",
    "AXM-CAP-PHYSICS-TRANSLATION-MOUNTS"
  ],
  "relationships": [
    {
      "type": "composes",
      "target": "AXM-CAP-PHYSICS-DISTANCE-JOINTS"
    },
    {
      "type": "composes",
      "target": "AXM-CAP-PHYSICS-TRANSLATION-MOUNTS"
    },
    {
      "type": "preserves",
      "target": "capabilities/physics-core/source/axm-physics-core.js",
      "basis": "The imported donor source remains unchanged; the composer performs one donor-core integration between additive pre- and post-constraint stages."
    }
  ],
  "implementation": {
    "kind": "DETERMINISTIC_SOURCE",
    "entrypoint": "capabilities/physics-core/uc-constraint-composer.js",
    "selftest": "node capabilities/physics-core/constraint-composer-selftest.js"
  },
  "family_order": [
    "translation-mounts",
    "distance-joints"
  ],
  "bounds": {
    "max_family_passes_per_stage": 16,
    "max_position_iterations_per_family_pass": 32,
    "max_velocity_iterations_per_family_pass": 32,
    "max_position_tolerance": 1000000,
    "max_velocity_tolerance": 1000000
  },
  "defaults": {
    "early_exit": true,
    "position_tolerance": 1e-8,
    "velocity_tolerance": 1e-8
  },
  "limitations": [
    "supports only the existing translation-mount and center-to-center distance-joint families",
    "fixed deterministic family order introduces ordering bias",
    "conflicting constraints are bounded by pass counts and are not claimed to converge globally",
    "early exit means only that measured position and constrained relative-velocity residuals satisfied configured thresholds at a pass boundary; it is not proof of equilibrium or global convergence",
    "no angular inertia or rotating local anchors",
    "no hinge, slider, weld, motor or gear constraints",
    "connected constrained bodies can still collide unless caller filters suppress that pair",
    "post-core projection can move bodies after donor-core contact evidence was generated",
    "not scientific validation"
  ],
  "tests": [
    {
      "command": "node capabilities/physics-core/constraint-composer-selftest.js",
      "proves": [
        "translation mounts and distance joints share one donor-core integration per composed step",
        "post-core mixed-family stabilization repairs gravity-induced drift for independent constraints",
        "family passes and nested solver iterations remain bounded",
        "already-satisfied constraints deterministically stop after one pass when early exit is enabled",
        "disabling early exit executes the full bounded pass budget and preserves the same settled final state",
        "conflicting constraints consume their configured pass budget instead of being falsely reported as converged",
        "position and constrained relative-velocity residuals are both visible in convergence evidence",
        "core-stage contact evidence remains separately inspectable",
        "same-runtime deterministic replay"
      ]
    }
  ],
  "root_fit": {
    "truth": {
      "fit": true,
      "basis": "The composer names its fixed ordering bias, bounded convergence limits, tolerance-based early-exit meaning and core-stage contact evidence boundary rather than presenting composition as a complete rigid-body solver."
    },
    "agency": {
      "fit": true,
      "basis": "Callers explicitly choose constraint data, bounded pass/iteration budgets, whether early exit is enabled, and the convergence tolerances used to skip redundant passes."
    },
    "continuity": {
      "fit": true,
      "basis": "The donor core and both existing constraint capabilities are reused without replacement; the optimization is confined to additive orchestration and diagnostics."
    },
    "wisdom-before-speed": {
      "fit": true,
      "basis": "The optimization skips work only after both positional and constrained-velocity residual checks pass; unresolved or conflicting constraints retain the full bounded budget rather than being declared solved for speed."
    }
  }
}
