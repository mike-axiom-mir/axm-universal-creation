{
  "id": "AXM-CAP-RENDER-ASSET-PREVIEW",
  "version": "0.1.0",
  "status": "live",
  "purpose": "render asset preview using native offline material machinery and exact artifact observation",
  "handles": [
    "render-asset-preview"
  ],
  "input_contract": {
    "required": [
      "path",
      "asset"
    ]
  },
  "output_contract": {
    "kind": "material-station-artifact-or-measurement"
  },
  "dependencies": [],
  "implementation": {
    "kind": "DETERMINISTIC_SOURCE",
    "entrypoint": "builtin:render_asset_preview",
    "source": "src/axm_uc/material_pipeline.py"
  },
  "limitations": [
    "Native bounded PNG/core glTF subset. No automatic aesthetic, seamlessness or target-engine acceptance.",
    "Sources are preserved; existing outputs are refused."
  ]
}
