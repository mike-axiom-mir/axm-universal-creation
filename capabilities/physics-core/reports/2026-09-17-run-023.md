# Physics Core Hourly Growth — Run 023

## Repository state

PR #169 was already merged at `2a798836378d47a787221597aa8fece0fd637e6a`. No competing open physics PR was found and no later hourly-physics successor branch existed, so this run established one successor from that exact `main`: `chatgpt/hourly-physics-growth-11`.

## Bounded improvement

Extend the optional orthogonal two-projection preflight with the complementary radial-minimum proof.

For one canonical body pair, if exactly two projected translation groups are unit-length and mutually orthogonal within the existing strict tolerance, and both projected intersections have finite lower and upper bounds, compute each projection's maximum possible absolute magnitude and then:

`maximum possible distance = hypot(max |projection A|, max |projection B|)`

If a same-pair radial minimum exceeds that maximum beyond the existing preflight tolerance, report `DISTANCE_MIN_EXCEEDS_ORTHOGONAL_PROJECTION_MAX`. The opt-in guard blocks that locally proven contradiction before donor integration. Accepted inputs still delegate through the established base guard/composer path exactly once.

## Truth boundary

This is a local 2D orthonormal-basis contradiction proof, not global satisfiability. A single bounded projection still cannot contradict a radial minimum because perpendicular freedom remains. Oblique or merely near-orthogonal directions are not combined. The proof does not combine more than two projections at once, different body pairs, triangles, loops, convergence, stability, physical correctness, 3D physics, or scientific validation.

## Source integrity

`capabilities/physics-core/source/axm-physics-core.js` remains byte-for-byte blob `b21b5d71f93c532b26e580766a7f5505024c8cfd`. The seven-family composer, donor step, solver ordering, and collision behavior are unchanged.

## Executable evidence

Updated the existing orthogonal preflight, its fail-closed guard, and its dedicated selftest. Regression coverage now includes a bounded `[-2,2] x [-2,2]` projected box against radial minimum `3` (provable conflict), radial minimum `2.8` (accepted), the one-projection radial-minimum non-proof, unchanged-world blocking, accepted-path delegation, and deterministic receipts, while preserving the previous 3-4-vs-4.5 radial-maximum proof.

## Verification

PR workflow verification is required for the final branch head. No success is claimed here until the current head is observed green.
