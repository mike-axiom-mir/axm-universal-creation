# UC Physics Growth — Run 036

## Lane state

PR #185 remains the only open physics-growth PR on `chatgpt/hourly-physics-growth-18`; the branch listing still shows no later hourly-physics successor. `main` advanced independently through PR #187 to `5bdeea950ed1292de23f65012d66a876ecf5c094`, so this lane was reconciled onto that exact main state with a two-parent merge before the new physics change.

## Bounded improvement

The finite two-projection radial-envelope helper now recognizes the exact case where one finite projection interval collapses to a single value while the other remains an interval. For a tolerance-eligible non-exact-orthonormal pair, that feasible inverse-basis envelope is a segment rather than a 2D parallelogram.

The helper now evaluates the segment directly: radial minimum needs one scaled point-to-segment calculation, radial maximum needs only the two unique endpoint norms, and an origin-contained segment needs zero edge-distance work. This replaces the previous four-edge/four-corner scan of duplicated geometry. The receipt exposes `degenerateProjectionIntervals`, `distanceMethod`, and `distanceWork` so the structural reduction is deterministic evidence rather than a wall-clock benchmark claim.

The change is intentionally bounded to exactly one degenerate projection interval. The established two-degenerate point-envelope path is not silently rewritten in this run.

## Executable evidence

A dedicated `one-degenerate-projection-envelope-selftest.js` verifies:

- fixed-first-projection segment geometry uses one edge-distance and two endpoint norms;
- the symmetric fixed-second-projection case uses the same bounded work;
- an origin-contained segment proves radial minimum zero with no edge-distance evaluation and two endpoint norms;
- the existing two-degenerate point behavior remains unchanged;
- helper evidence replays deterministically.

Physics Core Graft CI now has a dedicated step for this regression.

## Truth boundary

Direction eligibility is unchanged. The stronger preflight still combines exactly two normalized 2D projection directions on one canonical body pair only when they satisfy its configured strict orthogonality tolerance. This run only reduces repeated local work after eligible finite inverse-basis geometry is already being analyzed. It does not combine oblique directions outside eligibility tolerance, more than two projections in one proof, different body pairs, triangles, loops, global satisfiability, convergence, stability, physical correctness, 3D physics, or scientific validation. Geometry remains deterministic JavaScript `Number` arithmetic under the caller's contradiction tolerance.

## Source integrity

The imported donor `capabilities/physics-core/source/axm-physics-core.js` remains byte-for-byte blob `b21b5d71f93c532b26e580766a7f5505024c8cfd`. Solver, composer, collision path, guards, and donor integration behavior are unchanged.

## Verification

The helper and dedicated regression were syntax/execution checked locally before commit. Final GitHub workflow status is intentionally reported from evaluation of the exact PR head rather than pre-claimed here.
