# Physics Core hourly growth — Run 027

## Repository state

PR #175 remains the only open physics PR. `main` advanced from its original base to `9332e3455092cf8b0011fd69607d99a1e82a2dc8` through unrelated normalized skin-weight receiver work, so this lane was reconciled onto that exact main commit before further physics changes. No later hourly-physics successor branch existed.

## Bounded improvement

Reduce repeated lookup work inside the optional orthogonal projection/radial preflight without changing which proofs are eligible.

- Build one deterministic index of non-conflicting projection groups by canonical body pair per analysis.
- Reuse the matching pair bucket for each radial group instead of rescanning every projection group for every radial group.
- Expose `projectionPairBuckets` in deterministic counts/evidence so the lookup path is auditable.
- Bump the orthogonal preflight receipt to v0.5 and add a dedicated two-body-pair regression proving pair isolation, deterministic replay, and the one-index-per-analysis evidence path.
- Wire the new regression into Physics Core Graft CI.

This removes the repeated full projection scan per radial group; it is a structural lookup-cost improvement, not a benchmarked wall-clock speed claim. The within-pair two-projection combination loop is intentionally unchanged.

## Truth boundary

The proof remains limited to exactly two same-body-pair normalized unit directions mutually orthogonal within the configured strict tolerance. Indexing does not make oblique or near-orthogonal directions eligible, does not combine more than two projected directions at once, and does not add cross-pair, triangle, loop, global satisfiability, convergence, stability, physical correctness, 3D physics, or scientific-validation claims. Solver, guard, composer, collision, and donor integration behavior are unchanged.

## Source integrity

`capabilities/physics-core/source/axm-physics-core.js` is not modified. The platform donor remains the established byte-for-byte source; this run changes only the orthogonal preflight wrapper/evidence path, its regression, workflow verification, and this report.

## Verification

PR #175's prior head passed Physics Core Graft #144 and all other associated workflows before this continuation. The new pair-index regression is now part of Physics Core Graft and must pass on the final report-bearing head before this run is claimed green.
