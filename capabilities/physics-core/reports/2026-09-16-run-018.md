# Physics growth run 018 — conservative constraint preflight

## Repository state

- PR #164 was already merged into `main` at `091c90047a38894ebdb88dba268f1212411a6cc4`.
- No competing open physics PR was found before establishing successor branch `chatgpt/hourly-physics-growth-6`.
- PRs #140, #148, #161 and #163 remain completed predecessors; #160 remains the separate fixed-direction-lock composer integration predecessor.
- The imported donor `capabilities/physics-core/source/axm-physics-core.js` is not modified by this run.

## Bounded improvement

Add `uc-constraint-preflight.js`, a deterministic advisory correctness pass for constraints that can be reduced to the same exact normalized 1D translation projection.

The preflight reuses existing normalizers and intersects enabled intervals from:

- translation mounts (x and y exact offsets),
- axis locks,
- axis limits,
- fixed-direction locks,
- fixed-direction limits.

Body-order reversal and direction-sign reversal are canonicalized before grouping. A conflict is reported only when constraints on the same exact normalized projection have an empty interval intersection beyond the configured bounded tolerance.

Disabled analyzed constraints are still validated for body/source integrity but are excluded from intersections. Distance joints and distance limits are counted and reported as unsupported by this v0.1 local proof rather than being silently classified.

## Truth boundary

This is a conservative local preflight, not a global constraint solver or proof of satisfiability, convergence, stability, production readiness or scientific validity. Near-parallel but non-identical directions are deliberately kept separate. The pass is advisory and does not mutate world state, solver order, collision behavior or constraints.

## Evidence added

- `capabilities/physics-core/uc-constraint-preflight.js`
- `capabilities/physics-core/constraint-preflight-selftest.js`
- `capabilities/candidates/AXM-CAP-PHYSICS-CONSTRAINT-PREFLIGHT.json`
- capsule export through `capabilities/physics-core/index.js`
- `Physics Core Graft` CI coverage

Local syntax/JSON checks were completed before publication. GitHub CI status belongs to the exact PR head and should be inspected before any green claim.
