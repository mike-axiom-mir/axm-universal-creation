# UC Physics Growth — Run 034

## Lane state

PR #181 merged into `main` at `7edbc9544d52207a6f09cb85889d1587e22d4442`. A fresh open-PR and branch overlap check found no competing physics successor, so this run establishes exactly one lane: `chatgpt/hourly-physics-growth-18`.

## Bounded improvement

The finite two-projection radial-envelope helper now has a strict exact-orthonormal fast path. When both represented directions have exact unit squared norm and exact zero dot product, the inverse transform is an isometry: radial minimum and maximum come directly from the two projection intervals instead of running four point-to-segment distance evaluations and four corner-norm evaluations. Corners are reconstructed through the transpose basis, so evidence geometry remains available.

The fast path is deliberately exact-only. Tolerance-accepted near-orthogonal directions retain the established inverse-basis parallelogram path and all existing singular-value/tolerance safeguards. The helper exposes `distanceMethod` plus deterministic `distanceWork` counters so the structural work reduction is testable without making wall-clock benchmark claims.

## Executable evidence

`two-projection-radial-envelope-selftest.js` now verifies:

- exact axis-aligned orthonormal envelopes use zero edge-distance and zero corner-norm scans;
- a determinant `-1` swapped-axis orthonormal basis reconstructs the same exact geometry correctly;
- short-edge and `1e200` exact-axis cases preserve their established results on the fast path;
- tolerance-eligible but not exactly orthonormal directions remain on the general four-edge/four-corner path;
- all existing integrated local-conflict, fail-closed world-state, oblique-exclusion, range-hardening and deterministic replay checks remain present.

## Truth boundary

This is a common-case performance optimization inside the already-bounded two-projection helper. It does not widen direction eligibility, combine more than two projections, combine different body pairs, infer triangles or loops, prove global satisfiability, convergence, stability, physical correctness, 3D physics, or scientific validation. Geometry remains deterministic JavaScript `Number` arithmetic under the caller's existing contradiction tolerance.

## Source integrity

The imported donor `capabilities/physics-core/source/axm-physics-core.js` remains byte-for-byte blob `b21b5d71f93c532b26e580766a7f5505024c8cfd`; this run changes wrapper/test/report files only.

## Verification

The existing Physics Core Graft workflow already executes `two-projection-radial-envelope-selftest.js`, so the new regression is covered without adding another workflow surface. Final workflow status is intentionally reported from GitHub's evaluation of the PR head rather than pre-claimed here.
