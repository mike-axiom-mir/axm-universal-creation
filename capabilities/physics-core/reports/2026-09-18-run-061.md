# Physics core hourly growth — Run 061

## Starting state

- Predecessor PR #214 merged into `main` at `f7434152295bc9a3d57c59767481b2ef042ec675`.
- Run 060 exact head `2ffc6e1235526a07c0f1d7c69de0382b89600350` is confirmed green across Tests #764, Physics Core Graft #220, Physics radial-envelope receipt verifier #15, Candidate workspace isolation #580, Profession crew verification #314, Candidate adoption binding #584, and Candidate adoption resume #591.
- Fresh overlap inspection found no competing open physics PR and no existing `chatgpt/hourly-physics-growth-40` branch before this successor lane was created.

## Bounded improvement

Add `uc-two-projection-radial-envelope-projection-margins-verifier.js`, an independent read-only verifier for Run 060's represented radial-extremum projection-margin receipts.

The verifier first requires the existing independent active-set verifier to pass over the same finite two-projection inputs. It then independently recomputes each extremum witness projection from fresh direction-point dot products and reconstructs signed margins to both represented interval endpoints, local tolerance bands, signed feasibility margins, nearest represented-boundary gaps, ordered active sides, exact-degenerate flags, boundary/interior classifications, strict-interior flags, support flags, and extremum-level aggregate margins/counts.

The producer receipt's embedded active-set verification must exactly match a fresh verification. Numeric geometry is compared under the caller-selected JavaScript Number tolerance; schema identity, top-level reason, support flags, booleans, strings, counts, and ordered active-side evidence must match exactly.

This closes the immediate evidence gap left by Run 060: projection margins are no longer accepted only because the producer derived them; they now have an independently reconstructed receipt-consistency boundary.

## Executable verification

`radial-envelope-projection-margins-verifier-selftest.js` covers:

- exact-orthonormal boundary-margin verification;
- strictly interior origin verification;
- tolerance-eligible inverse-basis verification;
- degenerate segment and point verification;
- deterministic replay;
- projection tampering;
- signed endpoint/feasibility margin tampering;
- tolerance-band tampering;
- boundary/interior and strict-interior tampering;
- active-side and degenerate-flag tampering;
- support-flag and top-level reason tampering;
- extremum aggregate margin/count tampering;
- embedded active-set-verification tampering;
- schema tampering;
- missing projection-margin receipt rejection;
- underlying active-set tampering; and
- invalid-tolerance rejection.

The dedicated Physics radial-envelope receipt verifier workflow now runs this independent verifier immediately after the projection-margin derivation selftest, while retaining the existing generic receipt, optimization-plan, witness, independent witness-verifier, active-set, independent active-set-verifier, classification-matrix, representation-invariance, positive-scale covariance, interval-inclusion, and positive direction-scale representation-invariance suites.

## Truth boundary

Direction eligibility is unchanged: the caller proof still combines exactly two same-body-pair normalized 2D projection directions mutually orthogonal within the configured strict tolerance.

Projection margins remain represented signed-projection diagnostics. They are not world-space distances unless a represented direction is unit length, and verified margin consistency does not define collision contacts, contact normals, penetration depths, impulses, solver manifolds, physical correctness, 3D physics, gameplay correctness, or scientific validation.

Geometry and verification remain deterministic JavaScript Number arithmetic under explicit caller tolerances, not exact-arithmetic computational geometry or arbitrary-magnitude numerical-stability evidence.

## Source integrity

The imported platform donor `capabilities/physics-core/source/axm-physics-core.js` is intentionally untouched and remains blob `b21b5d71f93c532b26e580766a7f5505024c8cfd`.
