# Physics Core Hourly Growth — Run 050

Date: 2026-09-18
Lane: `chatgpt/hourly-physics-growth-29`
Base main commit: `9609998db6677391766e9ee7ff53a5b9b08a3cb9`

## Bounded improvement

Added `uc-two-projection-radial-envelope-plan-verifier.js`, a read-only verifier that runs only after the existing generic radial-envelope receipt verifier succeeds and then independently derives the expected public optimization plan from the same finite two-direction / two-interval inputs.

It checks:

- producer schema identity;
- `basisWork` classification counts;
- `geometryWork.projectionPointEvaluations` for point, segment, exact-central-symmetry, and general rectangles;
- `distanceMethod` selection;
- `distanceWork.edgeDistanceEvaluations` and `distanceWork.cornerNormEvaluations`.

The verifier treats these counters as deterministic declarative receipt evidence, not measured runtime instructions or hardware performance counters.

## Executable evidence

Added `radial-envelope-plan-verifier-selftest.js` covering exact orthonormal, scaled exact-orthogonal, tolerance-eligible nonzero-dot inverse-basis, centrally symmetric inverse-basis, origin-symmetric segment, and exact-origin point fixtures. The test deliberately tampers with distance method, distance work, geometry work, basis work, producer schema, and radial geometry so each independent rejection boundary is exercised.

The dedicated `Physics radial-envelope receipt verifier` workflow now runs both the generic geometry receipt selftest and the optimization-plan receipt selftest when either verifier or either test changes.

## Truth boundary

No direction eligibility changed. The stronger caller proof remains exactly two normalized 2D projection directions on one canonical body pair within the configured strict orthogonality tolerance. This run verifies receipt-plan consistency only and does not add arbitrary oblique proof, more than two projections, different body pairs, triangles, loops, global satisfiability, convergence, stability, physical correctness, 3D physics, gameplay correctness, or scientific validation.

Geometry and receipt verification remain deterministic JavaScript `Number` arithmetic under explicit caller tolerances.

## Source integrity

`capabilities/physics-core/source/axm-physics-core.js` remains byte-for-byte blob `b21b5d71f93c532b26e580766a7f5505024c8cfd` on this lane.

## Prior exact-head evidence

Merged Run 049 head `ba7c02dc794e0827fbd46712fbc48815559cd6d9` is confirmed green across Tests #752, Physics Core Graft #208, Physics radial-envelope receipt verifier #3, Candidate workspace isolation #568, Profession crew verification #302, Candidate adoption binding #572, and Candidate adoption resume #579.

## Current verification

GitHub CI should be read from the exact report-bearing head before claiming this run green.
