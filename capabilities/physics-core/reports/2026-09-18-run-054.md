# Physics Core Hourly Growth — Run 054

## State

PR #207 merged into `main` at `5c5d2cfdc3aa4e9462fd4d5ec5bc7874f12674a4`. Fresh overlap inspection found no competing open physics PR and no existing `chatgpt/hourly-physics-growth-33` branch, so this successor lane starts from that exact main state.

The merged Run 053 exact head `967b656a772b2c042ba13183907ca9f166aa684f` was confirmed green across Tests #756, Physics Core Graft #212, Physics radial-envelope receipt verifier #7, Candidate workspace isolation #572, Profession crew verification #306, Candidate adoption binding #576, and Candidate adoption resume #583 before merge.

## Bounded improvement

Added `radial-envelope-interval-inclusion-selftest.js`, a deterministic set-inclusion / radial-extrema monotonicity regression over the established 196-case finite/invertible radial-envelope matrix.

For each base basis/interval case, the test constructs four larger represented projection rectangles by outward-padding the first interval, second interval, or both intervals with bounded finite values. Across 784 expanded cases, it requires:

- producer support and deterministic replay;
- generic geometry receipt verification;
- independent optimization-plan receipt verification;
- every original evidence corner to remain feasible under the expanded represented intervals;
- radial minimum to never increase when the feasible set grows;
- radial maximum to never decrease when the feasible set grows;
- unchanged represented determinant and basis-work classification because directions are unchanged;
- exact agreement between independently derived optimization-plan receipts and producer method/work receipts after expansion.

The expansion matrix deliberately includes both small padding (`0.5`) and larger padding (`4`) so some one-sided and degenerate interval cases cross origin-containment and public distance-method classification boundaries. The regression asserts that at least one such method transition and one origin-containment transition are actually exercised; it does not assume optimization classifications stay fixed when the feasible set itself changes.

The dedicated Physics radial-envelope receipt verifier workflow now runs this interval-inclusion regression alongside the focused geometry/tamper tests, optimization-plan tests, 196-case classification matrix, representation-invariance matrix, and positive-scale covariance matrix.

## Truth boundary

Direction eligibility is unchanged. The stronger caller proof still combines exactly two normalized 2D projection directions on one canonical body pair within the configured strict orthogonality tolerance. This run strengthens deterministic set-inclusion evidence for already-supported finite 2D projection envelopes only; it does not authorize arbitrary oblique directions, more than two projections, different body pairs, triangles, loops, global satisfiability, convergence, stability, physical correctness, 3D physics, gameplay correctness, or scientific validation.

The monotonicity checks are bounded to the finite exercised matrix and deterministic JavaScript `Number` arithmetic under explicit `1e-9` comparison tolerance. They are not exact-arithmetic computational-geometry or arbitrary-magnitude numerical-stability claims.

## Source integrity

The imported donor `capabilities/physics-core/source/axm-physics-core.js` remains intentionally untouched and still has blob `b21b5d71f93c532b26e580766a7f5505024c8cfd`.
