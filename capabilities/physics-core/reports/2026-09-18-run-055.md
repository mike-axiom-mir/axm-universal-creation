# Physics Core Hourly Growth — Run 055

## State

PR #208 merged into `main` at `724dde638763253ba1d4cf93d13aaa9a4981e4bf`. Fresh overlap inspection found no competing open physics PR and no existing `chatgpt/hourly-physics-growth-34` branch, so this successor lane starts from that exact main state.

The merged Run 054 exact head `2c2811ac1cf9cd66ec02006f76b443d767a51de0` is confirmed green across Tests #757, Physics Core Graft #213, Physics radial-envelope receipt verifier #8, Candidate workspace isolation #573, Profession crew verification #307, Candidate adoption binding #577, and Candidate adoption resume #584.

## Bounded improvement

Added `radial-envelope-direction-scale-invariance-selftest.js`, a deterministic metamorphic regression for positive rescaling of the two represented projection directions together with the corresponding signed projection intervals.

The test reuses the established four represented basis classes and seven interval classes per axis (196 finite/invertible base cases), then applies four exact power-of-two representation transforms per base case for 784 transformed cases. Each transform multiplies one or both represented direction vectors by a positive factor and multiplies the matching projection interval by the same factor. That changes the numerical representation but encodes the same feasible world-space set.

Every transformed case requires:

- producer support and deterministic replay;
- generic geometry receipt verification;
- independent optimization-plan receipt verification;
- invariant radial minimum and maximum under explicit `1e-9` JavaScript `Number` comparison tolerance;
- invariant world-space evidence-corner coordinates;
- represented determinant scaling by the product of the two direction factors;
- unchanged interval-derived structural receipt fields;
- unchanged geometry-work and distance-work classifications;
- exact independent-plan agreement with the producer's basis, geometry, method, and distance-work receipts.

The matrix intentionally crosses the exact-orthonormal versus scaled-exact-orthogonal optimization boundary. It therefore requires at least one public `distanceMethod` transition and at least one `basisWork` transition while the represented world-space geometry remains invariant. This checks that optimization labels may legitimately change with representation even when the feasible set does not.

The dedicated Physics radial-envelope receipt verifier workflow now runs this regression alongside the focused verifier/tamper tests, optimization-plan tests, 196-case classification matrix, representation-invariance matrix, positive-scale covariance matrix, and interval-inclusion matrix.

## Truth boundary

Direction eligibility is unchanged. The stronger caller proof still combines exactly two normalized 2D projection directions on one canonical body pair within the configured strict orthogonality tolerance. This run tests helper-local representation equivalence only after already-supported finite invertible geometry is admitted; it does not widen caller eligibility or authorize arbitrary unnormalized or oblique proof directions.

It does not combine more than two projected directions, different body pairs, triangles, loops, global satisfiability, convergence, stability, physical correctness, 3D physics, gameplay correctness, or scientific validation. The checks remain bounded deterministic JavaScript `Number` evidence under explicit tolerance, not exact-arithmetic computational geometry or arbitrary-magnitude numerical-stability proof.

## Source integrity

The imported donor `capabilities/physics-core/source/axm-physics-core.js` remains intentionally untouched and still has blob `b21b5d71f93c532b26e580766a7f5505024c8cfd`.
