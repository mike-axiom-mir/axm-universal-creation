# Physics Core Hourly Growth — Run 057

## State

PR #210 merged into `main` at `57467114a95b7fbee22c9b82b9d64f29ab0a395e`. Fresh overlap inspection found no competing open physics PR and no existing `chatgpt/hourly-physics-growth-36` branch before this successor lane was created.

Run 056 exact head `a833e273b05e3bd3da0e8182e4051695e0398945` is now confirmed green across Tests #759, Physics Core Graft #215, Physics radial-envelope receipt verifier #10, Candidate workspace isolation #575, Profession crew verification #309, Candidate adoption binding #579, and Candidate adoption resume #586.

## Bounded improvement

Added `uc-two-projection-radial-envelope-witness-verifier.js`, a read-only independent verifier for Run 056's deterministic radial-extremum witness receipt. The verifier first reruns the established generic radial-envelope receipt verifier against the same directions, intervals, analysis, and caller tolerance, then requires the witness receipt's embedded base receipt to match that fresh result exactly.

For non-origin radial minima it independently recomputes the closest point to the world-space origin on all four returned boundary segments, including deterministic lowest-edge-index selection on an exact tie. For origin-contained envelopes it requires the exact origin witness and null boundary metadata. For maxima it independently recomputes all four corner norms and requires the deterministic lowest-corner-index exact-tie result. It also verifies stored witness projections against fresh dot products, checks both witness points remain feasible in the represented projection intervals, and checks point norms against both stored witness distances and the producer radial extrema under the caller-selected JavaScript `Number` tolerance.

Added `radial-envelope-witness-verifier-selftest.js` covering valid exact orthonormal, origin-contained tie, tolerance-eligible inverse-basis, degenerate segment, and degenerate point receipts plus deterministic replay. Tamper cases exercise minimum-point corruption, minimum segment-parameter corruption, maximum-corner tie-break corruption, stored projection corruption, embedded base-receipt corruption, invalid underlying radial evidence, missing witness evidence, and invalid numeric tolerance.

The dedicated Physics radial-envelope receipt verifier workflow now runs this independent witness-verifier regression alongside the existing focused geometry/tamper, optimization-plan, witness derivation, classification, representation-invariance, positive-scale covariance, interval-inclusion, and positive direction-scale representation-invariance suites.

## Truth boundary

Direction eligibility is unchanged. The stronger caller proof still combines exactly two normalized 2D projection directions on one canonical body pair within the configured strict orthogonality tolerance. The new verifier only checks internal consistency of an already-supported finite/invertible radial-envelope witness receipt; it does not admit additional directions, body pairs, or proof forms.

The verified points remain geometric radial-extremum witnesses for the represented affine envelope. They are not collision contacts, penetration vectors, impulses, solver guarantees, global satisfiability evidence, convergence/stability proof, physical correctness, 3D physics, gameplay correctness, or scientific validation. Arithmetic remains deterministic JavaScript `Number` arithmetic under an explicit caller tolerance rather than exact-arithmetic computational geometry.

## Source integrity

The imported donor `capabilities/physics-core/source/axm-physics-core.js` remains intentionally untouched and still has blob `b21b5d71f93c532b26e580766a7f5505024c8cfd`.
