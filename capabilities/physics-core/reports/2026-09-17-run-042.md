# Physics Core Hourly Action Report — Run 042

Date: 2026-09-17
Lane: `chatgpt/hourly-physics-growth-22`
Base: `main` merge commit `452b179cccff8acdde8930f7bde8662e52f86949`

## State reconciliation

PR #191 merged into `main`. A fresh open-PR search found no competing physics PR, and the hourly branch scan ended at `chatgpt/hourly-physics-growth-21`, so Run 042 established exactly one successor lane from the latest main commit.

The Run 041 exact PR head was subsequently confirmed green across Tests, Physics Core Graft, Candidate workspace isolation, Profession crew verification, Candidate adoption binding, and Candidate adoption resume.

## Bounded improvement

The finite two-projection radial-envelope helper now reduces radial-maximum work whenever at least one non-degenerate represented projection interval is exactly symmetric about zero and the already-accepted inverse basis has a finite nonzero represented direction dot product.

For world displacement `x = c1*u + c2*v`, squared radius is a positive-definite quadratic with cross term `2*u*v*(c1·c2)`. In 2D, `c1·c2 = -(d1·d2)/det(A)^2`, so the represented direction-dot sign determines which sign of an exactly symmetric coordinate maximizes the cross term. The other coordinate uses its endpoint with greatest absolute magnitude. Therefore one deterministically selected corner is the exact radial maximum; no four-corner comparison is needed.

This reduces the prior centrally symmetric case from 2 corner norms to 1 and extends the same one-corner maximum reduction to rectangles with exactly one origin-symmetric projection interval. Exact-zero-dot bases still use the earlier zero-corner scaled-orthogonal path. Near-symmetric intervals retain the established full scan.

## Verification added

`origin-symmetric-projection-envelope-selftest.js` now checks positive and negative represented dot signs, both-symmetric intervals, first-only and second-only symmetric intervals, authoritative agreement with all four corner radii, deterministic replay, and near-symmetric fallback to four corner norms.

GitHub exact-head verification is intentionally not claimed until the PR workflows complete.

## Truth boundary

Direction eligibility is unchanged: the stronger preflight still combines exactly two normalized 2D projection directions on one canonical body pair only when they satisfy the configured strict orthogonality tolerance. This run only reduces repeated local radial-maximum work inside already-supported finite invertible geometry. It adds no arbitrary oblique proof, more-than-two projection proof, cross-pair reasoning, triangles, loops, global satisfiability, convergence, stability, physical-correctness claim, 3D physics, or scientific validation.

The imported platform donor source remains outside this helper change and must stay byte-for-byte unchanged.
