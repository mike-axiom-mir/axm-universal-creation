# Physics Core Hourly Growth — Run 053

## State

PR #206 merged into `main` at `5c772b65eeba75abd0eb7a6c9c471b2d9975019d`. Fresh overlap inspection found no competing open physics PR and no existing `chatgpt/hourly-physics-growth-32` branch, so this successor lane starts from that exact main state.

The merged Run 052 exact head `973e89274fde50a132e03e11edeea225d71f9f7b` is confirmed green across Tests #755, Physics Core Graft #211, Physics radial-envelope receipt verifier #6, Candidate workspace isolation #571, Profession crew verification #305, Candidate adoption binding #575, and Candidate adoption resume #582.

## Bounded improvement

Added `radial-envelope-scale-covariance-selftest.js`, a deterministic positive-scale metamorphic regression over the existing 196-case finite/invertible radial-envelope classification matrix.

For every base case, the test scales both signed projection intervals by four exact positive power-of-two factors: `0.125`, `0.5`, `2`, and `8`. With the represented directions held fixed, the accepted inverse/orthogonal basis maps that interval scaling to the same feasible world-space envelope scaled uniformly about the origin. Across all 784 scaled cases, the test requires:

- producer support and deterministic producer replay;
- generic geometry receipt verification;
- independent optimization-plan receipt verification;
- radial minimum and maximum equal to the base values multiplied by the same positive scale under explicit `1e-9` JavaScript Number tolerance;
- every returned world-space evidence corner equal to the corresponding base corner multiplied by the same scale;
- unchanged determinant and interval-derived structural receipt classification;
- unchanged public `distanceMethod`, `basisWork`, `geometryWork`, and `distanceWork` classifications;
- exact agreement between independent plan receipts and producer work/method receipts after scaling.

The dedicated Physics radial-envelope receipt verifier workflow now runs this scale-covariance regression alongside the focused geometry/tamper tests, optimization-plan tests, 196-case classification matrix, and representation-invariance matrix.

## Truth boundary

Direction eligibility is unchanged. The stronger caller proof still combines exactly two normalized 2D projection directions on one canonical body pair within the configured strict orthogonality tolerance. This run adds deterministic positive-scale covariance evidence only; it does not authorize arbitrary oblique directions, more than two projections, different body pairs, triangles, loops, global satisfiability, convergence, stability, physical correctness, 3D physics, gameplay correctness, or scientific validation.

The covariance claim is bounded to finite represented values in the exercised matrix and deterministic JavaScript `Number` arithmetic under the explicit comparison tolerance. It is not an exact-arithmetic computational-geometry claim and does not imply numerical stability for arbitrary magnitudes.

## Source integrity

The imported donor `capabilities/physics-core/source/axm-physics-core.js` remains intentionally untouched by this run.
