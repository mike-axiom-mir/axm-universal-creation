# Physics Core hourly growth — Run 026

## Repository state

PR #173 was already merged. This successor lane starts from current `main` at `e768b4810bfa9da09c9b927bb719c363e0494612` after checking for open physics PRs and later hourly-physics branches; none were found.

## Bounded improvement

Harden and streamline the internal full-precision exact-geometry view used by the optional orthogonal projection/radial preflight.

- Clamp direct exact-geometry `tolerance` inputs to the same `[0, BasePreflight.MAX_TOLERANCE]` contract as the conservative base preflight, with the same default/fallback semantics.
- Materialize projection groups and radial groups exactly once per analysis and reuse those arrays for both report output and counts instead of recomputing both grouping passes.
- Add a dedicated deterministic selftest covering default, negative, non-finite and oversized tolerance handling plus group/count consistency and replay.
- Wire that selftest into Physics Core Graft CI.

## Truth boundary

This is a correctness/performance repair to evidence preparation only. It does not change the seven-family composer, guard decisions for already-valid bounded inputs, solver order, collision behavior, donor integration, or any physical capability. The orthogonal stronger layer remains limited to two same-body-pair normalized unit directions that are mutually orthogonal within its strict tolerance. No oblique or near-orthogonal combination, more-than-two projected-direction proof, different-pair reasoning, triangles, loops, global satisfiability, convergence, stability, physical correctness, 3D physics, or scientific validation is claimed.

## Source integrity

`capabilities/physics-core/source/axm-physics-core.js` remains byte-for-byte blob `b21b5d71f93c532b26e580766a7f5505024c8cfd` and is not modified by this lane.

## Verification

Pending PR CI after opening the successor pull request. The branch comparison currently contains only the exact-geometry helper repair, its dedicated selftest, this report, and the Physics Core Graft workflow hook.
