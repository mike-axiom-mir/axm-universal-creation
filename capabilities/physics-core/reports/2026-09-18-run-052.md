# Physics Core Hourly Growth — Run 052

## State

PR #205 merged into `main` at `ce70d717e381df6ca8a27c0c9fabe9d48bb1b23c`. Fresh overlap inspection found no competing open physics PR and no existing `chatgpt/hourly-physics-growth-31` branch, so this successor lane starts from that exact main state.

The merged Run 051 exact head `596a56ee4ce839e27931937c43d3aae6a104052b` is confirmed green across Tests #754, Physics Core Graft #210, Physics radial-envelope receipt verifier #5, Candidate workspace isolation #570, Profession crew verification #304, Candidate adoption binding #574, and Candidate adoption resume #581.

## Bounded improvement

Added `radial-envelope-representation-invariance-selftest.js`, a deterministic metamorphic regression over the existing 196-case finite/invertible radial-envelope classification matrix.

For every base case, the test applies four exact representation transforms:

- negate the first projection direction and map its interval to `[-max, -min]`;
- negate the second projection direction and map its interval to `[-max, -min]`;
- swap the two projection directions and their intervals;
- rotate both represented directions by an exact quarter turn while leaving projection intervals unchanged.

The first three transforms encode the same feasible world-space set through an equivalent projection representation. The quarter-turn transform rotates the feasible world-space set by an orthogonal map, which preserves Euclidean distance from the origin. Across all 784 transformed cases, the test requires:

- producer support and deterministic replay;
- generic geometry receipt verification;
- independent optimization-plan verification;
- radial minimum and maximum agreement with the base case under explicit `1e-9` JavaScript Number tolerance;
- unchanged public `distanceMethod`, `basisWork`, `geometryWork`, and `distanceWork` classifications;
- exact agreement between the independent transformed plan receipt and the transformed producer receipt fields.

The dedicated Physics radial-envelope receipt verifier workflow now runs this representation-invariance regression alongside the focused receipt tests, optimization-plan tests, and 196-case classification matrix.

## Truth boundary

Direction eligibility is unchanged. The stronger caller proof still combines exactly two normalized 2D projection directions on one canonical body pair within the configured strict orthogonality tolerance. This run adds deterministic representation-invariance evidence only; it does not authorize arbitrary oblique directions, more than two projections, different body pairs, triangles, loops, global satisfiability, convergence, stability, physical correctness, 3D physics, gameplay correctness, or scientific validation.

All comparisons remain deterministic JavaScript `Number` arithmetic under explicit caller tolerances, not exact-arithmetic computational geometry.

## Source integrity

The imported donor `capabilities/physics-core/source/axm-physics-core.js` remains intentionally untouched by this run.
