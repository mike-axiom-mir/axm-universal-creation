# Physics Core Hourly Action Report — Run 040

## Lane
- PR #190 merged into `main` at `50e85c8f6a39822eef01e4fb546dcb875917bb3b`.
- Fresh checks found no open physics PR and no later hourly-physics branch.
- Exactly one successor was created from that merge state: `chatgpt/hourly-physics-growth-21`.

## Bounded improvement
Reduced radial-minimum edge work for the remaining non-degenerate finite two-projection envelope case where neither signed projection interval contains zero.

Both intervals are then strictly one-sided. Because the inverse basis is linear and Euclidean radius is positively homogeneous, any feasible projection point that lies away from both interval boundaries nearest zero can be scaled toward projection-space zero until one of those two nearest boundaries is reached; the mapped world displacement and its radius shrink by the same factor. Therefore the two farther projection boundaries cannot contain the radial minimum.

The helper now evaluates exactly two edges instead of four for this case: the nearest-to-zero boundary edge of the first projection interval and the nearest-to-zero boundary edge of the second projection interval. Evidence exposes:
- `distanceMethod: inverse-basis-parallelogram-two-active-edges`
- `distanceWork.edgeDistanceEvaluations: 2`
- `distanceWork.cornerNormEvaluations: 4`

The established zero-containing, degenerate, exact-orthonormal, and exact origin-symmetry paths retain their prior behavior.

## Verification
- Added `two-active-edge-projection-envelope-selftest.js` covering positive/positive, negative/negative, and mixed-sign one-sided intervals.
- The regression independently computes all four edge distances and verifies that the new two-edge result matches the authoritative four-edge minimum and replays deterministically.
- Added that regression to Physics Core Graft CI.
- Exact-head GitHub Actions status is intentionally not pre-claimed in this report; it must be read after the PR is opened.

## Truth boundary
Direction eligibility is unchanged. The stronger preflight still combines exactly two normalized 2D projection directions on one canonical body pair and only when they satisfy the configured strict orthogonality tolerance. This run only reduces repeated local edge-distance work after already-supported finite invertible geometry has been accepted.

It adds no oblique reasoning outside eligibility tolerance, no more-than-two projection proof, no different-body-pair combination, no triangles, loops, global satisfiability, convergence, stability, physical correctness, 3D physics, or scientific validation. Geometry remains deterministic JavaScript `Number` arithmetic under the caller's contradiction tolerance.

## Source integrity
The imported donor `capabilities/physics-core/source/axm-physics-core.js` remains byte-for-byte blob `b21b5d71f93c532b26e580766a7f5505024c8cfd`. No donor source, solver, composer, collision path, guard, or integration behavior was changed.
