# Physics growth run 020 — same-pair radial conflict preflight

## Repository/lane state

- PR #166 was already merged and remains a completed predecessor.
- Current `main` at lane creation: `ecef151548736628cff62652a36be6eb1c7b1ad6` (merge of PR #166).
- No competing open PR matching physics work was found before writing.
- Successor lane: `chatgpt/hourly-physics-growth-8`.

## Bounded improvement

Extend the conservative constraint preflight from exact projected-translation interval intersections to also analyze center-distance joints and distance limits that reference the exact same canonical body pair.

The preflight now normalizes all seven composer families, converts enabled distance joints to exact radial intervals `[length,length]`, converts enabled distance limits to `[minLength,maxLength]`, canonicalizes reversed body order without sign inversion, and reports `CONFLICTING_DISTANCE_INTERVALS` only when the same-pair radial intervals have an empty intersection beyond the bounded tolerance.

The opt-in fail-closed guard consumes the stronger preflight: a proven same-pair radial contradiction is blocked before donor integration with `coreStepExecuted:false`, while compatible distance constraints still delegate exactly once to the existing composer.

## Truth boundary

This is not a global constraint satisfiability solver. It does not reason across triangles, loops, multi-pair geometry, coupled projected/radial constraints, convergence, stability, physical correctness, 3D physics or scientific validation. Near-parallel directions remain separate. Disabled constraints are validated but excluded from intersections.

No solver ordering or donor-core behavior is changed. `capabilities/physics-core/source/axm-physics-core.js` remains byte-for-byte preserved.

## Verification target

- `node capabilities/physics-core/constraint-preflight-selftest.js`
- `node capabilities/physics-core/constraint-preflight-guard-selftest.js`
- existing Physics Core Graft workflow on the successor PR

Local GitHub network access from the execution container was unavailable, so no clone-based runtime pass is claimed here; executable verification is delegated to the repository workflow after push.
