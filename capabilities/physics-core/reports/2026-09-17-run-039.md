# Physics Core Hourly Action Report — Run 039

## Lane
- PR #189 merged into `main` at `c9d28d43e4b25532fcd31622196a08794e467ade`.
- Fresh checks found no open physics PR and no later hourly-physics branch.
- Exactly one successor was created from that merge state: `chatgpt/hourly-physics-growth-20`.

## Bounded improvement
Reduced repeated radial-maximum work for an already-supported finite two-projection envelope case.

For a non-degenerate inverse-basis projection rectangle whose two represented signed intervals are both exactly symmetric about zero, the feasible world-space parallelogram is centrally symmetric about the origin. Opposite corners are negatives of each other under the linear inverse basis and therefore have equal radii.

The helper now evaluates only two unique corner norms instead of four while retaining `minimumDistance = 0` without edge scans. Evidence exposes:
- `centrallySymmetricProjectionRectangle: true`
- `distanceMethod: inverse-basis-parallelogram-origin-symmetric`
- `distanceWork.edgeDistanceEvaluations: 0`
- `distanceWork.cornerNormEvaluations: 2`

Merely near-symmetric represented intervals retain the established four-corner scan.

## Verification
- Added `origin-symmetric-projection-envelope-selftest.js` covering the two-corner work reduction, equality of opposite-corner radii, deterministic replay, and near-symmetric fallback.
- Added that regression to Physics Core Graft CI.
- Exact-head GitHub Actions status is intentionally not pre-claimed in this report; it must be read after the PR is opened.

## Truth boundary
Direction eligibility is unchanged. The stronger preflight still combines exactly two normalized 2D projection directions on one canonical body pair and only when they satisfy the configured strict orthogonality tolerance. This run only reduces repeated local norm work after already-supported finite invertible geometry has been accepted.

It adds no oblique reasoning outside eligibility tolerance, no more-than-two projection proof, no different-body-pair combination, no triangles, loops, global satisfiability, convergence, stability, physical correctness, 3D physics, or scientific validation. Geometry remains deterministic JavaScript `Number` arithmetic under the caller's contradiction tolerance.

## Source integrity
The imported donor `capabilities/physics-core/source/axm-physics-core.js` remains byte-for-byte blob `b21b5d71f93c532b26e580766a7f5505024c8cfd`. No donor source, solver, composer, collision path, guard, or integration behavior was changed.
