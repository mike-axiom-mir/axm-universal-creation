# Physics Core Hourly Growth — Run 015

## Baseline

- PR #148 is merged and remains the completed predecessor lane.
- PR #160 subsequently merged fixed-direction locks into the six-family one-step composer on `main`.
- No open competing physics-growth PR was found before writing.
- Successor branch: `chatgpt/hourly-physics-growth-3`, created from then-current `main` at `f5ccd414fc1824d0f810440caee38764c946a281`.

## Bounded improvement

Close the explicit wrapper gap left after fixed-direction locks entered the composer: make `directionLocks` a first-class family in the guarded activity route and in deterministic connected-component collision isolation.

Implemented:

- activity gate family registry now includes `directionLocks`;
- disabled fixed-direction locks remain normalized/validated evidence but are excluded from solver residuals and isolation topology before delegation;
- enabled fixed-direction-lock edges can join deterministic collision-isolation components;
- temporary negative collision groups remain caller-opt-in, component-wide, deterministically allocated and restored after the donor-core stage;
- no second donor-core integration step is introduced;
- candidate contracts now describe the six-family guarded path;
- `direction-lock-integration-selftest.js` covers disabled inertness, body-reference validation, active topology, temporary collision suppression, group restoration, one-step delegation, projection residuals and deterministic replay;
- Physics Core Graft CI now runs that integration regression.

## Truth boundary

This does **not** add a rotating/local-axis prismatic joint. Direction locks still constrain only one fixed world-space projection and intentionally leave perpendicular translation free. Component isolation is broader than physical correction: when explicitly requested, a direction-lock edge joins the whole connected component for collision suppression even though perpendicular translation remains unconstrained. Existing nonzero caller collision-group semantics remain fail-safe and are not silently overwritten. No angular inertia, rotating anchors, angular limits, motors, hinges, gears, deformables, fluids, 3D physics or scientific validation is claimed.

## Source integrity

`capabilities/physics-core/source/axm-physics-core.js` was not edited. The donor blob observed on the baseline main tree was `b21b5d71f93c532b26e580766a7f5505024c8cfd`.

## Verification

A dedicated executable regression was added and wired into the existing Physics Core Graft workflow. Final CI status is recorded from GitHub Actions after the successor PR is opened; no queued or in-progress check is treated as passed.
