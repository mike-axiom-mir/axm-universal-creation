# Physics growth run 019 — fail-closed preflight guard

## Repository state

- PR #165 was already merged into `main` at `2595788885faca5fbe69e1497b55d535aff51259`.
- No competing open physics PR was found before establishing successor branch `chatgpt/hourly-physics-growth-7` / PR #166.
- PRs #140, #148, #161, #163, #164 and #165 remain completed predecessors; #160 remains the separate fixed-direction-lock composer integration predecessor.
- The imported donor `capabilities/physics-core/source/axm-physics-core.js` is not modified by this run and remains at blob `b21b5d71f93c532b26e580766a7f5505024c8cfd`.

## Bounded improvement

Add `uc-constraint-preflight-guard.js`, an opt-in fail-closed wrapper around the existing seven-family composer.

The guard first runs full composer validation and the conservative projected-translation preflight. Invalid full-composer inputs return `INVALID_CONSTRAINTS`; locally proven projected contradictions return `PROVABLE_LOCAL_CONFLICT`. Both blocked paths preserve the input world, report identical before/after world checksums, expose `coreStepExecuted:false`, and do not call the donor integration path.

Conflict-free valid input is delegated exactly once to the existing composer. The guard returns the existing composer receipt without altering its solver ordering or bounded-pass behavior. Valid distance joints and distance limits remain outside the local projected-conflict proof and are delegated rather than silently treated as conflicts.

The guard also emits a deterministic decision checksum. Its evidence-copy path preserves non-finite interval endpoints such as `Infinity` instead of collapsing them through JSON serialization.

## Truth boundary

This is not a new constraint solver. It is an optional execution gate over existing validation, preflight and composer behavior. A successful preflight does not prove global satisfiability, convergence, stability, physical correctness, production readiness or scientific validity. Distance families remain outside the local projected-conflict proof. Existing composer contact-evidence and ordering boundaries remain unchanged.

## Evidence and verification

- `capabilities/physics-core/uc-constraint-preflight-guard.js`
- `capabilities/physics-core/constraint-preflight-guard-selftest.js`
- `capabilities/candidates/AXM-CAP-PHYSICS-CONSTRAINT-PREFLIGHT-GUARD.json`
- capsule export through `capabilities/physics-core/index.js`
- dedicated `Physics Core Graft` CI coverage

Physics Core Graft run #133 passed the initial guard implementation including the dedicated guard regression. After repairing evidence cloning so unbounded interval endpoints remain lossless, exact code head `c76abbe3f2e77ebb59859b53bffa032ab2043d0e` passed Physics Core Graft run #134 completely. Candidate workspace isolation also passed on that exact code head. Broader Tests, candidate adoption and profession-crew workflows were still in progress at this report point and are not claimed as passed.

The local execution container could not resolve github.com, so no clone-based local runtime result is claimed; GitHub Actions is the executable verification source for this run.
