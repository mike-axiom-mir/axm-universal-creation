# Physics Core Hourly Growth — Run 022

## Repository state

PR #168 was already merged at `ae76436052a13e2d9214ba527c9b84e1cffc622c`. No competing open physics PR was found, so this run established one successor lane from that exact `main`: `chatgpt/hourly-physics-growth-10`.

## Bounded improvement

Add an optional stronger same-body-pair preflight layer that can combine exactly two projected translation intervals when their normalized 2D directions are unit-length and mutually orthogonal within a strict tolerance.

For each such pair with a finite same-body-pair radial maximum, compute the minimum magnitude required by each projected interval and the resulting orthonormal lower bound:

`minimum distance = hypot(min |projection A|, min |projection B|)`

If that lower bound exceeds the finite radial maximum beyond the existing preflight tolerance, report `ORTHOGONAL_PROJECTIONS_EXCEED_DISTANCE_MAX`. Cases where either single projection already exceeds the radial maximum are left to the established PR #168 proof so evidence is not duplicated.

An opt-in guard blocks only these newly proven contradictions before donor integration and otherwise delegates to the established fail-closed guard/composer chain. Accepted inputs therefore do not gain a second solver path or donor step.

## Truth boundary

This deliberately expands one previous limitation without claiming general satisfiability. It does not combine oblique or merely near-orthogonal directions, more than two projected directions, different body pairs, radial minima, triangles, loops, multi-pair geometry, convergence, stability, physical correctness, 3D physics, or scientific validation. The existing conservative preflight remains unchanged; this is an explicit stronger optional layer.

## Source integrity

`capabilities/physics-core/source/axm-physics-core.js` is not modified. The platform donor remains byte-for-byte preserved unless a later grounded architecture change explicitly supersedes it.

## Executable evidence

Added:

- `capabilities/physics-core/uc-orthogonal-projection-preflight.js`
- `capabilities/physics-core/uc-orthogonal-projection-preflight-guard.js`
- `capabilities/physics-core/orthogonal-projection-preflight-selftest.js`
- capsule exports in `capabilities/physics-core/index.js`
- a dedicated Physics Core Graft workflow step

The regression covers a 3-4 projection pair against a 4.5 radial maximum, the exact 3-4-5 accepted boundary, oblique-direction exclusion, radial-minimum non-inference, fail-closed world preservation, accepted-path delegation, and deterministic report/decision replay.

## Verification target

- `node capabilities/physics-core/orthogonal-projection-preflight-selftest.js`
- existing Physics Core Graft workflow on the successor PR

No CI success is claimed until the pushed successor head is observed passing.
