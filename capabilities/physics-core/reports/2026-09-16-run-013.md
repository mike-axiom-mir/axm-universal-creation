# UC Physics Growth — Run 013

Date: 2026-09-16
Lane: `chatgpt/hourly-physics-growth-2` / PR #148

## Reconciliation

`main` advanced from `dde8d952...` to `8576e075...` through PR #158. The incoming changes were UV/bake padding and mesh-quality surfaces outside `capabilities/physics-core/**`. The persistent physics lane was reconciled first with explicit two-parent merge `b82970b...`; no second physics PR was opened.

## Change

Closed Run 012's explicit integration gap by adding **world-axis translation limits** as the fifth family in the shared one-step constraint stack.

Composer v0.5.0 now uses deterministic order:

`translation-mounts -> distance-joints -> distance-limits -> axis-locks -> axis-limits`

Axis limits use the existing bounded primitive. Their range interior stays slack; only a position outside the interval or velocity moving outward at an active boundary contributes correction/residual. The selected x/y axis remains world-space and orthogonal translation remains free.

The activity gate now keeps disabled axis limits validated/evidenced while excluding them from solve residuals and isolation topology. Component isolation now accepts enabled axis-limit edges; a slack axis-limit edge may join explicit isolation topology without being reinterpreted as a physical lock.

## Evidence

Added `axis-limit-integration-selftest.js` covering:

- exactly one donor-core integration through the composer;
- crossing an axis bound during the core stage and bounded post stabilization;
- outward boundary-velocity suppression with orthogonal freedom;
- genuinely slack motion inside the allowed range;
- explicit component collision isolation for an enabled axis-limit edge;
- disabled axis-limit exclusion through the activity gate;
- same-runtime deterministic replay.

`Physics Core Graft` now executes this integration test. Existing axis-lock/composer tests were updated for the appended fifth-family order without changing the prior four-family ordering.

## Truth boundary

This is not a full slider/prismatic joint. Axes do not rotate with bodies; no angular inertia, rotating anchors, angular limits, motors, hinges, welds, gears, deformables, fluids, 3D physics, cross-engine deterministic-equivalence claim, or scientific validation is implied.

Isolation topology and physical correction remain separate claims: an enabled slack axis-limit edge can opt a connected component out of self-collision while transmitting no axis-limit correction inside its range.

The standalone axis-limit entrypoint still performs its own donor-core step. Mixed-family callers must use the shared composer/activity/isolation path instead of chaining standalone wrappers and accidentally advancing physics twice.

The imported AXM Physics Core v0.3.1 donor source was not edited.

## Verification

At the code-bearing head `e85bba40...`, GitHub Actions created `Physics Core Graft` run #103 plus the repository-wide checks. They were queued at the evidence check, so this report does **not** claim those runs passed.
