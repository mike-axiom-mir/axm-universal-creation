# Physics Core Hourly Growth — Run 025

## Repository state

PR #172 was already merged and current `main` was `e6826acbc7296ba77d25534c8d3d3770ff3fa747`. No competing open physics PR or later hourly-physics successor branch was found, so this run established one successor from that exact `main`: `chatgpt/hourly-physics-growth-13`.

## Bounded improvement

Preserve an unrounded decision witness for each stronger orthogonal projection/radial proof while keeping the existing rounded receipt fields for concise presentation.

The v0.4 orthogonal preflight now records the exact compared values, proof margin, and active tolerance for both radial-maximum and radial-minimum combined proofs. This does not change conflict decisions, solver behavior, guard routing, or donor integration; it closes an evidence-integrity gap where a real full-precision conflict near a rounding boundary could display equal 1e-9-rounded summary values.

## Correctness cases

With zero proof tolerance, a 3-4 projection pair requires exact radial distance `5`, while a radial maximum `4.9999999996` is a real local contradiction even though both public summary values round to `5`. The new `decisionWitness` retains `5` versus `4.9999999996` plus the positive proof margin. The complementary radial-minimum regression uses a bounded orthogonal box with exact maximum distance `3` against radial minimum `3.0000000004`; rounded summaries both display `3`, while the witness preserves the actual positive contradiction margin.

## Truth boundary

The mathematical scope is unchanged: only two same-body-pair normalized unit directions that are mutually orthogonal within the configured strict tolerance are combined at once. Oblique or merely near-orthogonal directions, more than two projected directions at once, different body pairs, triangles, loops, global satisfiability, convergence, stability, physical correctness, 3D physics, and scientific validation remain outside the claim.

## Source integrity

`capabilities/physics-core/source/axm-physics-core.js` remains byte-for-byte blob `b21b5d71f93c532b26e580766a7f5505024c8cfd`. The seven-family composer, donor step, collision behavior, and existing guards are unchanged.

## Executable evidence

Updated `uc-orthogonal-projection-preflight.js` to schema/version v0.4 and extended `orthogonal-projection-preflight-selftest.js` with radial-maximum and radial-minimum rounding-boundary regressions plus exact witness assertions.

## Verification

PR workflow verification is required for the final branch head. No CI success is claimed in this report until the exact current head is observed green.
