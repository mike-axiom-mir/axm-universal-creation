# Physics growth run 021 — bounded projection/radial conflict preflight

## Repository/lane state

- PR #167 was already merged and remains a completed predecessor.
- Current `main` at lane creation: `bb090c470379542d38d24eea403832400c84a7b4` (merge of PR #167).
- No competing open PR matching physics work was found before writing; existing hourly physics branches are predecessor branches only.
- Successor lane: `chatgpt/hourly-physics-growth-9`.

## Bounded improvement

Extend the conservative constraint preflight with one additional locally provable same-body-pair contradiction. After the existing projected interval and radial interval intersections are each found internally compatible, the preflight may compare one normalized projected interval against a finite same-pair center-distance maximum.

For a unit projection direction, every valid relative position must satisfy `|projection| <= center distance`. The preflight therefore computes the minimum absolute value allowed by the projected interval. If that minimum required projection magnitude exceeds the finite radial maximum beyond the configured tolerance, it reports `PROJECTION_EXCEEDS_DISTANCE_MAX`.

The opt-in fail-closed guard consumes the stronger preflight and blocks that proven local contradiction before donor integration with `coreStepExecuted:false`. Compatible inputs still delegate exactly once to the established seven-family composer.

## Truth boundary

This remains a conservative local proof, not global constraint satisfiability. It does not combine two or more independent projected directions into a radial lower bound, does not treat radial minima as contradictions against a single projection because perpendicular freedom may satisfy them, and does not reason across triangles, loops, multi-pair geometry, convergence, stability, physical correctness, 3D physics or scientific validation.

No solver ordering, collision behavior or donor-core integration behavior is changed. `capabilities/physics-core/source/axm-physics-core.js` remains byte-for-byte preserved at blob `b21b5d71f93c532b26e580766a7f5505024c8cfd`.

## Verification target

- `node capabilities/physics-core/constraint-preflight-selftest.js`
- `node capabilities/physics-core/constraint-preflight-guard-selftest.js`
- existing Physics Core Graft workflow on the successor PR

The local execution container cannot resolve GitHub hosts, so no clone-based full runtime pass is claimed here. Both modified JavaScript files and selftests passed local `node --check`; executable integration verification is delegated to the repository workflow after push.
