# UC Physics Growth — Run 014

Date: 2026-09-16
Lane: `chatgpt/hourly-physics-growth-2` / PR #148

## Reconciliation

PR #148 remained open and mergeable. `main` remained at `8576e075bf92f2907b8e58b8445826fbcd713cf0`, already contained in this lane, so no merge/rebase was required. No competing open physics-growth PR was found.

## Change

Added standalone deterministic fixed-direction translation locks in `uc-direction-locks.js`.

A lock constrains the scalar projection of the relative body translation onto one caller-supplied finite non-zero world-space direction while leaving the perpendicular translation component free. Direction input is normalized deterministically. Position correction and velocity impulse are shared by inverse mass, bounded by existing-style correction/impulse limits, and executed in bounded pre/post passes around exactly one donor-core step.

Added `direction-locks-selftest.js`, physics-core export wiring, candidate registration, and Physics Core Graft CI coverage.

## Evidence

The regression covers arbitrary diagonal projection locking, perpendicular position/velocity freedom, direction normalization, equal-mass correction sharing, gravity drift plus post-core stabilization, immovable `NO_DYNAMIC_MASS` truth, disabled behavior, bounded iteration counts, invalid/zero direction rejection, and deterministic replay.

## Truth boundary

This is a fixed **world-space** direction constraint, not a rotating local-axis or full prismatic/slider joint. It has no angular inertia, rotating anchors, angular limits, motors, gears, deformables, fluids, 3D physics, cross-engine deterministic-equivalence claim, or scientific-validation claim.

Standalone v0.1 is deliberately not yet integrated into the mixed composer/activity/isolation stack. Callers requiring one donor-core step across mixed constraint families must not chain this step wrapper with another wrapper; integration remains a later explicit gate.

The imported donor source `capabilities/physics-core/source/axm-physics-core.js` was not edited.
