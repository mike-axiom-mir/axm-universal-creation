# UC Physics Core Hourly Action Report — Run 028

Date: 2026-09-17
Lane: `chatgpt/hourly-physics-growth-15`
PR: #177
Predecessor: merged PR #175 / `chatgpt/hourly-physics-growth-14`

## Repository state

- PR #175 was already merged before this run.
- `main` was inspected at merge commit `32fe27fa8f244839ee4c6a10c6219adcef2c065f`.
- No open physics PR and no later hourly-physics successor branch were found before creating this lane.
- This run therefore established one successor only: `chatgpt/hourly-physics-growth-15` / PR #177.

## Bounded improvement

Reduce repeated scalar work inside the optional same-pair orthogonal projection/radial preflight without changing proof eligibility or solver behavior.

- Compute each non-conflicting projection group's unit-length error, minimum absolute interval magnitude, and maximum absolute interval magnitude exactly once per analysis.
- Store those metrics beside the already-indexed canonical body-pair projection buckets.
- Reuse the metrics across every candidate projection pair instead of recomputing interval magnitudes and unit-length error inside the quadratic pair loop.
- Expose deterministic `projectionMetricRecords` and `projectionPairCandidates` counts so the structural work reduction is auditable rather than implied.
- Extend the existing pair-index selftest with a four-projection/six-candidate case proving four metric records are reused across six pair candidates, with only the two truly orthogonal pairs reaching radial proof checks.

This is a bounded structural work reduction. It is not a benchmarked wall-clock performance claim.

## Truth boundary

The stronger layer still combines only two normalized unit projections on the same canonical body pair when they are mutually orthogonal within the configured strict tolerance. It does not combine oblique or merely near-orthogonal directions, more than two projections in one proof, different body pairs, triangles, loops, global satisfiability, convergence, stability, physical correctness, 3D physics, or scientific validation.

The conservative base preflight, fail-closed guard, composer, collision behavior, and donor integration path are unchanged.

## Source integrity

`capabilities/physics-core/source/axm-physics-core.js` is unchanged and resolves to blob `b21b5d71f93c532b26e580766a7f5505024c8cfd` on both `main` and this successor branch.

## Verification

Physics Core Graft run #150 passed on executable/test head `dbd5d8f7168fa6067bfb4777c687d170fe5416e4`, including the extended `Verify pair-indexed orthogonal proof lookup` regression and every established physics-core gate. This report-only follow-up records that evidence; no executable, test, solver, guard, composer, collision, or donor source changed after the passing run.
