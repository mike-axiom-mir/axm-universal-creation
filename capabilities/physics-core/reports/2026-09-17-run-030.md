# Physics Growth Run 030 — 2026-09-17

## Repository state

PR #177 remains the single open hourly physics-growth lane. `main` advanced independently through PR #179 to `2295495cccf60c1fbd541b20215d17ac18e32821`; the lane was reconciled onto that exact state before this physics change. No later hourly-physics branch was present when the run began.

## Bounded improvement

Hardened the optional same-body-pair two-projection/radial contradiction proof at the configured orthogonality-tolerance edge.

The eligibility boundary is unchanged: two normalized unit directions must still belong to one canonical body pair and satisfy the existing strict orthogonality tolerance. Once a pair is eligible, radial bounds now use the exact singular values of that two-direction projection basis rather than treating a tolerance-accepted pair as perfectly orthonormal:

- radial-maximum proof lower bound: `||projection|| / sigma_max`;
- radial-minimum proof upper bound: `||projection|| / sigma_min` when the accepted two-direction basis is non-singular.

For exactly orthonormal inputs both singular values are `1`, so established Pythagorean results are unchanged. Near the tolerance boundary the bounds become conservatively weaker, preventing tiny numeric deviations from producing false local-conflict claims. Receipts retain full-precision singular values and projection bounds in each decision witness while rounded summary values remain presentation-only. The existing `proofGeometry` receipt contract remains `full-precision-normalized`; the new bound model is exposed separately as `radialBoundModel: singular-value-conservative`.

## Verification evidence

Added deterministic regressions for both directions of the tolerance-edge issue: a near-orthogonal pair whose raw `hypot` would falsely exceed a radial maximum, and a bounded near-orthogonal pair whose raw `hypot` would falsely fall below a radial minimum. Existing exact-orthogonal, indexing, metric-reuse, proof-budget, fail-closed and deterministic replay cases remain in the focused tests.

Physics Core Graft run #158 caught an evidence-contract break in the first draft: `proofGeometry` had been renamed even though its established meaning still applied. That was repaired by preserving the prior field and adding the separate `radialBoundModel` field instead. Physics Core Graft run #160 was queued for the repaired executable/test head `d469d6386321a04e249376cda405ab4b97c08b88` when this report was written; no green result is claimed here until GitHub reports one.

## Source integrity and truth boundary

`capabilities/physics-core/source/axm-physics-core.js` remains byte-for-byte donor blob `b21b5d71f93c532b26e580766a7f5505024c8cfd` on both `main` and this branch.

This remains a local 2D preflight proof only. It does not combine oblique or merely near-orthogonal directions outside the configured eligibility tolerance, more than two projected directions in one proof, different body pairs, triangles, loops, global satisfiability, convergence, stability, physical correctness, 3D physics, or scientific validation. The conservative base preflight, composer, donor core, and solver behavior are unchanged.