# Physics Growth Run 051 — deterministic radial-envelope classification matrix

## State
- Predecessor PR #204 merged into `main` at `3028557dbd01a1423210ab0b2a3b7f9bedc5e226`.
- Fresh overlap checks found no open competing physics PR and no existing `chatgpt/hourly-physics-growth-30` branch before this lane was created.
- Run 050 exact head `257db91e7f36be2d1a3f027b5d1ad61cb8c46a58` is confirmed green across Tests #753, Physics Core Graft #209, Physics radial-envelope receipt verifier #4, Candidate workspace isolation #569, Profession crew verification #303, Candidate adoption binding #573, and Candidate adoption resume #580.

## Bounded improvement
Added `radial-envelope-classification-matrix-selftest.js`, a deterministic cross-product regression over four represented basis classes and seven signed-projection interval classes on each axis: 196 accepted finite/invertible cases total.

Every case independently requires:
- deterministic producer replay;
- generic geometry receipt verification;
- optimization-plan receipt verification;
- deterministic verifier replay; and
- exact agreement between the independently derived public plan and the producer's `basisWork`, `geometryWork`, `distanceMethod`, and `distanceWork` receipts.

The matrix also asserts that it reaches all nine current public distance-method classifications plus the complete current 1/2/4 projection-reconstruction counts, 0/1/2 active-edge counts, 0/1/2/4 corner-norm counts, and 0/2 lazy direction-norm counts. The dedicated Physics radial-envelope receipt verifier workflow now runs this matrix alongside the existing focused geometry and tamper regressions.

## Truth boundary
This is stronger deterministic evidence for the existing finite two-projection helper contract only. It does not widen direction eligibility, combine arbitrary oblique directions, admit more than two projected directions, combine different body pairs, prove triangles or loops, establish global satisfiability, convergence, stability, physical correctness, 3D physics, gameplay correctness, or scientific validation. Geometry and verification remain JavaScript `Number` arithmetic under explicit caller tolerances.

## Source integrity
The imported donor `capabilities/physics-core/source/axm-physics-core.js` remains byte-for-byte blob `b21b5d71f93c532b26e580766a7f5505024c8cfd`.
