# Physics Core Hourly Action Report — Run 038

## Lane
- PR #185 had merged, placing `main` at `17b3533a73ae865080762cd9429a027baa5552b0`.
- Fresh checks found no open physics PR and no later hourly-physics branch, so exactly one successor was created: PR #189 / `chatgpt/hourly-physics-growth-19`.
- The successor starts from the exact PR #185 merge state; merged predecessor lanes were not reopened or duplicated.

## Bounded improvement
Reduced repeated radial-minimum edge work for one already-supported finite two-projection envelope case.

For a non-degenerate inverse-basis projection rectangle where exactly one signed projection interval contains zero and the other lies wholly on one side of zero, minimizing the positive-definite inverse-basis squared distance over the zero-containing coordinate leaves a convex function of the other projection coordinate with its global minimum at zero. Over a one-sided interval, the feasible minimum therefore lies on that interval's boundary edge nearest zero.

The helper now evaluates only that one active edge instead of all four parallelogram edges. Radial maximum still uses all four corner norms.

Evidence now reports:
- `zeroContainingProjectionIntervals: 1`
- `distanceMethod: inverse-basis-parallelogram-single-active-edge`
- `distanceWork.edgeDistanceEvaluations: 1`
- `distanceWork.cornerNormEvaluations: 4`

Exactly orthonormal, origin-contained, one-degenerate segment, two-degenerate point, and neither-interval-contains-zero paths retain their established behavior.

## Verification
- Added `single-zero-projection-envelope-selftest.js` covering both interval orientations, positive and negative nearest-boundary selection, deterministic replay, and preservation of the full four-edge scan when neither interval contains zero.
- Physics Core Graft #188 passed on code/workflow head `ab21150451be8a6cfe643c49b5a7b08d5187211b`, including the new dedicated regression and all established physics-core checks.
- Candidate workspace isolation #542 also passed on that head.
- Tests #726, Profession crew verification #276, Candidate adoption binding #546, and Candidate adoption resume #553 were still in progress at the latest inspection, so no broader all-green claim is made.
- This report commit follows the verified code/workflow head and therefore may trigger a new exact-head workflow run; no exact report-bearing-head green claim is made until a fresh check confirms it.

## Truth boundary
Direction eligibility is unchanged. The stronger preflight still combines exactly two normalized 2D projection directions on one canonical body pair and only when they satisfy the configured strict orthogonality tolerance. This change only reduces repeated local work after already-supported finite invertible geometry has been accepted.

It adds no oblique reasoning outside eligibility tolerance, no more-than-two projection proof, no different-body-pair combination, no triangles, loops, global satisfiability, convergence, stability, physical correctness, 3D physics, or scientific validation. Geometry remains deterministic JavaScript `Number` arithmetic under the caller's contradiction tolerance, not exact-arithmetic computational geometry.

## Source integrity
The imported platform donor source remains byte-for-byte blob `b21b5d71f93c532b26e580766a7f5505024c8cfd`. No donor source, solver, composer, collision path, guard, or integration behavior was changed.
