# Physics Core Hourly Action Report — Run 041

## Lane
- PR #191 remains the single open persistent physics-growth lane on `chatgpt/hourly-physics-growth-21`.
- `main` remained at `50e85c8f6a39822eef01e4fb546dcb875917bb3b` during the overlap check, so no successor lane was created.
- Fresh PR and branch checks found no competing open physics PR and no later hourly-physics branch.
- The prior exact head `437a25c50153b20029690a2f96d7b02f2997dfd4` is now confirmed green across Tests #731, Physics Core Graft #193, Candidate workspace isolation #547, Profession crew verification #281, Candidate adoption binding #551, and Candidate adoption resume #558.

## Bounded improvement
Extended the existing zero-work exact-orthonormal radial-envelope path to a still-strict exact represented orthogonal case where the two accepted directions have finite nonzero norms but are not represented as exactly unit length.

For an exactly represented orthogonal basis, the two projection components remain Euclidean-orthogonal after accounting for each direction's represented scale. Radial extrema therefore come directly from the projection rectangle as:
- `hypot(minAbs(first)/||first||, minAbs(second)/||second||)` for the radial minimum, and
- `hypot(maxAbs(first)/||first||, maxAbs(second)/||second||)` for the radial maximum.

This avoids all edge-distance and corner-norm radial scans for exact-zero-dot pairs whose tiny represented unit-length error previously sent them through the inverse-basis geometry path. The new receipt method is `orthogonal-scaled-projection-rectangle` with deterministic work of 0 edge-distance evaluations and 0 corner norms.

The path is intentionally strict: a tolerance-eligible pair with any nonzero represented dot product retains the established inverse-basis path.

## Verification
- Added `scaled-orthogonal-projection-envelope-selftest.js`.
- The regression uses direction scales whose squared unit errors remain within the caller's established `1e-9` eligibility tolerance while the represented dot product is exactly zero.
- It verifies the scale-adjusted minimum/maximum, zero radial scan work, deterministic replay, and fallback to the established two-active-edge inverse-basis path for a small nonzero represented dot product.
- Physics Core Graft CI now runs the regression directly.
- Exact-head GitHub Actions status for the Run 041 commits is intentionally not pre-claimed here; it must be read after the report-bearing head is exercised.

## Truth boundary
Direction eligibility is unchanged. The stronger preflight still combines exactly two normalized 2D projection directions on one canonical body pair and only when they satisfy the configured strict orthogonality tolerance. Run 041 adds no new eligible direction pair; it only reduces repeated local geometry work for the exact represented zero-dot subset already inside that boundary.

It adds no oblique reasoning outside eligibility tolerance, no more-than-two projection proof, no different-body-pair combination, no triangles, loops, global satisfiability, convergence, stability, physical correctness, 3D physics, or scientific validation. Geometry remains deterministic JavaScript `Number` arithmetic under the caller's contradiction tolerance.

## Source integrity
The imported donor `capabilities/physics-core/source/axm-physics-core.js` remains byte-for-byte blob `b21b5d71f93c532b26e580766a7f5505024c8cfd`. Donor source, solver, composer, collision path, guards, and integration behavior are unchanged.
