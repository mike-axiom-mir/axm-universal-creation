# Physics Core Hourly Growth — Run 024

## Repository state

PR #171 was already merged at `eb571ebd67b0e6c82387f1da32700e9be844b2af`. No competing open physics PR was found and no later hourly-physics successor branch existed, so this run established one successor from that exact `main`: `chatgpt/hourly-physics-growth-12`.

## Bounded improvement

Repair the optional orthogonal two-projection preflight so geometric proof decisions use full-precision normalized directions and interval intersections instead of the base preflight's 1e-9-rounded presentation summaries.

A new internal exact-geometry view reconstructs the same deterministic canonical projection/radial groups from the established seven normalized constraint families without mutating world state or executing the donor core. The orthogonal preflight keeps rounded values in its public evidence receipt, but orthogonality, projection magnitudes, radial bounds, and conflict decisions now use the unrounded normalized values.

## Correctness case

Before this repair, a normalized direction whose true dot product with X was slightly above the strict `1e-9` orthogonality tolerance could round to a displayed X component of exactly `1e-9`, allowing the stronger proof layer to treat that pair as orthogonal at the tolerance boundary. The regression uses direction `{x: 1.4e-9, y: 1}` against X: the rounded base receipt still displays `1e-9`, but the stronger preflight now correctly declines the orthogonal proof because the full-precision normalized dot product is above tolerance. The opt-in guard therefore does not block on presentation rounding.

## Truth boundary

This does not widen the mathematical claim. The stronger layer still combines only two same-body-pair unit directions that are mutually orthogonal within the configured strict tolerance. It does not combine oblique or merely near-orthogonal directions, more than two projected directions at once, different body pairs, triangles, loops, global satisfiability, convergence, stability, physical correctness, 3D physics, or scientific validation. Rounded public receipt values remain presentation evidence only.

## Source integrity

`capabilities/physics-core/source/axm-physics-core.js` remains byte-for-byte blob `b21b5d71f93c532b26e580766a7f5505024c8cfd`. The seven-family composer, donor step, solver ordering, and collision behavior are unchanged.

## Executable evidence

Added `uc-exact-projection-geometry.js`, updated `uc-orthogonal-projection-preflight.js` to use it for proof decisions, and extended `orthogonal-projection-preflight-selftest.js` with the near-orthogonal rounding-boundary case plus accepted guard delegation. Existing 3-4-5 radial-maximum and bounded radial-minimum proofs remain covered.

## Verification

PR workflow verification is required for the final branch head. No success is claimed here until the current head is observed green.
