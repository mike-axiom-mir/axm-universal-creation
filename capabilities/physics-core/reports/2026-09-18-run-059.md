# Physics growth Run 059 — independent radial-envelope active-set verification

Date: 2026-09-18

## Reconciliation

PR #212 merged into `main` at `376b5c49a8bdbff344fe20e61cd89a73b8a96c75`. A fresh overlap check found no competing open physics PR and no existing `chatgpt/hourly-physics-growth-38` branch, so this run continues the single persistent lane from that exact main state.

The exact Run 058 head `2721c48c7f634e6014c5d5b98918f5fdad0f1bd5` is now confirmed green across Tests #761, Physics Core Graft #217, Physics radial-envelope receipt verifier #12, Candidate workspace isolation #577, Profession crew verification #311, Candidate adoption binding #581, and Candidate adoption resume #588.

## Bounded improvement

Add `uc-two-projection-radial-envelope-active-set-verifier.js`, a read-only independent verifier for Run 058's extremum active projection-set receipt.

The verifier:

- reruns the independent radial-envelope witness verifier before trusting active-set evidence;
- requires the active-set receipt's embedded witness-verification evidence to match that fresh verification exactly;
- independently recomputes minimum and maximum witness projections from fresh direction-point dot products;
- independently reclassifies represented interval endpoint activity under the caller-selected `numericTolerance`;
- checks projection values, active flags, ordered active-side lists, exact-degenerate interval flags, witness kinds, and active-axis counts;
- independently enforces that a non-origin boundary minimum activates at least one represented projection axis and a corner maximum activates both represented projection axes.

Add `radial-envelope-active-set-verifier-selftest.js` covering valid orthonormal, interior-origin, tolerance-eligible inverse-basis, degenerate-segment, degenerate-point, and deterministic-replay cases, plus deliberate tampering of projection evidence, active-side lists, active flags, active-axis counts, degenerate flags, witness kinds, embedded witness verification, underlying witness evidence, missing active-set receipts, and invalid tolerances.

The dedicated `Physics radial-envelope receipt verifier` workflow now runs this independent active-set verification alongside the existing generic geometry/tamper, optimization-plan, witness derivation, independent witness-verifier, active-set derivation, classification, representation-invariance, positive-scale covariance, interval-inclusion, and positive direction-scale representation-invariance suites.

## Source integrity

`capabilities/physics-core/source/axm-physics-core.js` remains byte-for-byte blob `b21b5d71f93c532b26e580766a7f5505024c8cfd` on the successor branch.

## Truth boundary

Direction eligibility is unchanged. The caller proof still combines exactly two same-body-pair normalized 2D projection directions mutually orthogonal within the configured strict tolerance.

Run 059 verifies deterministic internal consistency of represented projection-side activity around already-verified radial-extremum witnesses. These active sets remain tolerance-relative JavaScript Number geometry metadata. They are not collision contacts, contact normals, penetration constraints, impulses, solver manifolds, physical-validation evidence, 3D physics, gameplay correctness, or scientific validation.

Do not infer arbitrary oblique combinations outside the existing eligibility tolerance, more than two projected directions in one proof, different body pairs, triangles, loops, global satisfiability, convergence, stability, physical correctness, or arbitrary-magnitude numerical stability.
