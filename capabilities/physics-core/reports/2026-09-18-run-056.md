# Physics Core Hourly Growth — Run 056

## State

PR #209 merged into `main` at `520584c87071837e8dc1ff9a39da140f5efa229d`. Fresh overlap inspection found no competing open physics PR and no existing `chatgpt/hourly-physics-growth-35` branch before this successor lane was created.

Run 055 exact head `662d0a1b0155b47b1344d6beb64d0108a0300cbd` is confirmed green across Tests #758, Physics Core Graft #214, Physics radial-envelope receipt verifier #9, Candidate workspace isolation #574, Profession crew verification #308, Candidate adoption binding #578, and Candidate adoption resume #585.

## Bounded improvement

Added `uc-two-projection-radial-envelope-witness.js`, a read-only deterministic witness layer for the already-supported finite two-projection radial envelope. It first requires the existing generic receipt verifier to pass, then derives explicit world-space witnesses for both radial extrema without changing producer geometry or direction eligibility.

For the radial minimum, if both represented projection intervals contain zero, the exact witness is the world-space origin. Otherwise the helper recomputes the closest point to the origin on all four returned boundary segments and deterministically selects the lowest edge index on an exact tie. For the radial maximum, it recomputes all four returned corner norms and deterministically selects the lowest corner index on an exact tie. Both witness distances must agree with the producer's scalar radial extrema under the caller-selected JavaScript `Number` tolerance.

Added `radial-envelope-witness-selftest.js` covering an exact orthonormal boundary minimum, an origin-contained symmetric envelope with deterministic maximum-corner tie breaking, a tolerance-eligible inverse-basis envelope, a degenerate segment, a degenerate point, deterministic replay, tampered radial-evidence rejection, and invalid-tolerance rejection.

The dedicated Physics radial-envelope receipt verifier workflow now runs the witness regression alongside the existing focused geometry/tamper tests, optimization-plan tests, classification matrix, representation-invariance matrix, positive-scale covariance matrix, interval-inclusion matrix, and direction-scale representation-invariance matrix.

## Truth boundary

Direction eligibility is unchanged. The stronger caller proof still combines exactly two normalized 2D projection directions on one canonical body pair within the configured strict orthogonality tolerance. The witness helper only operates after the existing finite/invertible envelope receipt has independently verified; it does not admit additional directions, body pairs, or geometric proof forms.

The returned points are geometric extrema witnesses for the represented affine envelope only. They are not collision contacts, penetration vectors, impulses, solver guarantees, global satisfiability evidence, convergence/stability proof, physical correctness, 3D physics, gameplay correctness, or scientific validation. Arithmetic remains deterministic JavaScript `Number` arithmetic under an explicit caller tolerance rather than exact-arithmetic computational geometry.

## Source integrity

The imported donor `capabilities/physics-core/source/axm-physics-core.js` remains intentionally untouched and still has blob `b21b5d71f93c532b26e580766a7f5505024c8cfd`.
