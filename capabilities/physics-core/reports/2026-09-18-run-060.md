# Physics core hourly growth — Run 060

## Starting state

- Predecessor PR #213 merged into `main` at `722b0925635960b48c02247e6e0381380f3f7d7e`.
- Run 059 exact head `5b9da6dddf572f416d2975a660b504d1796bf795` is confirmed green across Tests #762, Physics Core Graft #218, Physics radial-envelope receipt verifier #13, Candidate workspace isolation #578, Profession crew verification #312, Candidate adoption binding #582, and Candidate adoption resume #589.
- Fresh overlap inspection found no competing open physics PR and no existing `chatgpt/hourly-physics-growth-39` branch before this successor lane was created.

## Bounded improvement

Add `uc-two-projection-radial-envelope-projection-margins.js`, a read-only diagnostic/evidence layer over the independently verified radial-envelope active-set receipt.

For both radial-extremum witnesses and both represented projection axes, the helper recomputes the witness projection from a fresh direction-point dot product and reports signed represented-projection margins to the interval `min` and `max` endpoints. It also records the local tolerance band, signed feasibility margin, nearest represented-boundary gap, exact-degenerate status, active sides, boundary/interior classification, and strict-interior status.

The helper requires the independent active-set verifier to pass first. A witness projection lying outside an interval beyond the caller-selected JavaScript Number tolerance is rejected. Freshly reconstructed active sides must exactly match the independently verified active-set evidence.

This makes tolerance-sensitive boundary evidence inspectable without converting projection-space margins into unsupported world-space or physical quantities.

## Executable verification

`radial-envelope-projection-margins-selftest.js` covers:

- exact-orthonormal boundary margins;
- strictly interior origin margins;
- origin-on-boundary margins;
- tolerance-eligible inverse-basis geometry;
- degenerate segment margins;
- degenerate point margins;
- deterministic replay;
- tampered active-set rejection; and
- invalid-tolerance rejection.

The dedicated Physics radial-envelope receipt verifier workflow now runs this selftest alongside the existing generic receipt, optimization-plan, witness, independent witness-verifier, active-set, independent active-set-verifier, classification-matrix, representation-invariance, positive-scale covariance, interval-inclusion, and positive direction-scale representation-invariance suites.

## Truth boundary

Direction eligibility is unchanged: the caller proof still combines exactly two same-body-pair normalized 2D projection directions mutually orthogonal within the configured strict tolerance.

Projection margins are represented signed-projection diagnostics. They are not world-space distances unless a represented direction happens to be unit length, and they are not collision contacts, contact normals, penetration depths, impulses, solver manifolds, physical-validation evidence, 3D physics, gameplay correctness, or scientific validation.

Geometry and verification remain deterministic JavaScript Number arithmetic under explicit caller tolerances, not exact-arithmetic computational geometry or arbitrary-magnitude numerical-stability evidence.

## Source integrity

The imported platform donor `capabilities/physics-core/source/axm-physics-core.js` is intentionally untouched and remains expected at blob `b21b5d71f93c532b26e580766a7f5505024c8cfd`.
