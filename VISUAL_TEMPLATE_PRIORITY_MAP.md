# AXM Visual Template Expansion Priority Map

Status: v9 growth lane after verified v1–v8 merges.

The visual-template fabric grows where Universal Creation is likely to reuse the knowledge soon. This is a build priority, not canon: templates remain editable foundations and may be replaced when evidence supports a better structure.

## Merged foundations

- `editor.creative.core` — professional source-first editing surfaces.
- `comic.narrative.core` — editable comics and visual narrative.
- `axm.system.shell` — AXM registry/state/evidence/workflow/recovery shell.
- `game.shared.core` plus racing/co-op/RTS/game-system foundations.
- `visual.keyart.core` — editable key art/poster/cover composition.
- `visual.cards.core` — editable cards/decks/collectibles.
- `visual.cinematic.core` — exact cinematic titles/credits/subtitles/timed overlays.
- `visual.broadcast.core` — source-truth video/program/stream overlays.

## Priority 5E — Evidence-aware infographic / diagram editor — IMPLEMENTED IN THIS LANE

Product: `visual.diagram.core`

Implemented screens:

- `visual.diagram.project-hub`
- `visual.diagram.canvas-editor`
- `visual.diagram.node-editor`
- `visual.diagram.relationship-editor`
- `visual.diagram.evidence-editor`
- `visual.diagram.annotation-editor`
- `visual.diagram.legend-style`
- `visual.diagram.layout-variants`
- `visual.diagram.review-compare`
- `visual.diagram.export`

New primitives:

- `diagram-node`
- `relationship-edge`
- `evidence-reference`
- `data-field`
- `annotation-pin`
- `legend-entry`
- `group-boundary`
- `layout-guide`
- `callout-card`
- `diagram-export-target`

Style: `visual.diagram.evidence`.

The central rule is semantic truth: visual proximity never becomes an undeclared relationship. Exact edge endpoints/type/direction, evidence status, values/units/periods and group membership remain editable source state. Layout variants may improve readability but cannot rewrite semantics.

## Next high-value visual/creation families

1. **World-map / lore-atlas editor** — regions, routes, layers, points of interest, time/state overlays and narrative references without invented coordinates or route truth.
2. **Interactive visual-novel surfaces** — dialogue, choices, character staging, backgrounds, history/log, saves and branching state.
3. **3D showroom / gallery presentation** — hero object, comparison, annotations, material/variant selection and orbit/detail views.
4. **Music visualizer / album-art editor** — cover systems, track identity, waveform/beat-driven visual states and export crops.

## Cross-cutting rules

Every family preserves the existing visual-template contract:

1. normalized responsive geometry with explicit compact/standard/wide behavior where relevant;
2. exact identity/version/digest through the Sticker Registry bridge;
3. no floating `latest` references or silent upgrades;
4. richer editable source remains authoritative over preview/export derivatives;
5. meaning, selection, ownership, errors and consequences do not depend on color alone;
6. `math_hooks` remain available for mathematical ratio/range growth without replacing template identity;
7. templates are known foundations, never automatic canon.

## Immediate build order

1. exact-head verify `visual.diagram.core`;
2. merge v9 only when template + repository checks pass;
3. begin world-map / lore-atlas editing from fresh main;
4. keep prioritizing reusable source-first editors over flat one-off outputs.
