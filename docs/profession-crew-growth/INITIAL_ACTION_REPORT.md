# Profession crews — initial action report

Date: 2026-09-16. Environment: Linux, Python 3.12.14.

Implemented professional station planning/execution, 18 pinned profession bodies
and workflows, source-preserving package data, actual artifact observers and
persistent scoped practice. Existing specialist tournaments can carry these
professional contracts and the same learned practice. No new profession was
needed and no imported profession was promoted above EXPERIMENTAL.

The working snapshot was refreshed from live UC source and manifests against
`49ef11ca42b2079dffbd595daa8ea8626b99d2ab`; all refreshed source/test/manifest
bytes matched the corresponding Git blob hashes. Before publication, the three
files introduced by main through `87f93e1a27b2e3414f6422cd38e31b00e89d6a56`
were also fetched. Unrelated unfetched donor/tool changes were not claimed as
locally tested; publication builds on the full remote main tree to preserve them.

## Tested

- **61 tests passed**: new crew suite and neighboring stepwise, specialist
  tournament, machine, procedural GLB and static asset target evidence tests.
- Demo: initial invalid Python artifact -> HOLD_FAILED_CHECK; repeated defect ->
  HOLD_LEARNED_PREFLIGHT with target absent; corrected input ->
  COMPLETE_BOUNDED_CHECKS; new verification -> PASS.
- Actual Python project -> QA verification handoff.
- Actual GLB artifact generation and re-verification; substitution with a different
  valid GLB rejected against the requested specification.
- Missing adapters/judgment, failed downstream dependencies, fabricated provider
  PASS, stale artifact/runtime/context, duplicate jobs/experience, interrupted
  writes, competing writers and protected-state/symlink cases.
- Wheel build and separate-process cold import; all 18 profession bodies plus
  bundled license/notice present in installed package data.
- Syntax compilation and diff whitespace checks passed.

## Limits

This proves the bounded deterministic machinery and its tested behavior. It does
not prove artistic/professional mastery, rendered visual quality, animation,
gameplay, audio, independent cognition or generalization outside exact contexts.
The full UC test suite and Windows were not run. CI is configured, not preclaimed.
Publishing this lane does not mean main has adopted it.

Local usage and the remaining real gaps are described in PROFESSION_CREWS.md.
The hourly continuation must follow HANDOFF.md and current repository evidence.
