# Manual improvement cycle 001 — target evidence and professional handoffs

Date: 2026-09-16. Environment: Linux, Python 3.12.14.
Authorization: Mike requested one improvement cycle now after the hourly task
creation was blocked by the 20-active-task limit. This is one manual cycle.

## Starting evidence

Initial crew branch head: `fef0834c07e207f6ee943a389f119a6c26fc4ddc`.
The first two regression tests reproduced the gap: no live route for
`verify-static-asset-target`, and a crew returned `HOLD_CAPABILITY_GAP` instead
of an evidence-specific hold and handoff.

Before publication, PR #143 was observed merged at main
`8dd55d7186aa99911ca8d28af303f31d2c05fad4`. GitHub's comparison reported no file
differences from the starting crew head; the merge preserves its exact tree
`0a4f48c0fc777564d84abf8f582f1bad55e52e0b`. This cycle builds on that main commit
and keeps the existing working branch. A successor PR is necessary because a
merged PR cannot accept further changes. Separate open PRs #140 (physics) and
#141 (aftertouch) were inspected; neither implements this connection.

## Changed behavior

- Reuse the existing static target evidence gate through a live, read-only
  `verify-static-asset-target` capability and automatic professional station.
- Compare the packet against independent job target/required-lane inputs.
  Missing requirements and wrong engine/version/context hold the job.
- Reopen actual GLB bytes for crew observations and fresh verification; do not
  trust provider PASS fields or accept a replacement artifact.
- Name owners for missing or insufficient evidence and include their original
  profession contracts in the plan. Missing collision -> gameplay engineer;
  navigation -> world/encounter designer; visual LOD -> art director.
- Preserve declared failures, stop downstream actions, and keep optional
  NOT_TESTED evidence visible without making it a new required gate.
- Exclude external target packets and their errors from practice updates.
  Existing locally observed project failures still activate learned preflight.

## Verification

```sh
PYTHONPATH=src:tests python -m unittest test_profession_crew test_profession_target_evidence test_stepwise_workflow test_specialist_tournament test_machine test_procedural_3d test_static_asset_target_evidence -q
python tools/profession_crew_demo.py
git diff --check
```

**77 selected tests passed**, including 16 new target-station regressions.
The demo performed a real invalid project -> learned preflight (destination
absent) -> corrected project -> fresh PASS sequence. It also generated a real
GLB and produced `HOLD_TARGET_EVIDENCE` with three correct specialist handoffs,
zero target practice entries and `independently_reproduced: false`.

PASS packet fixtures in tests are explicitly synthetic declarations. No actual
engine import, collision, navigation, visual inspection, performance test or
professional approval was performed. The full UC suite and Windows were not
tested. This report records local evidence; successor PR CI must be checked on
its published commit before reporting a remote pass. No new merge is claimed.

Next useful work is a real bounded target execution/observation adapter, or
profession fit improvements grounded in actual capability needs. Neither is
automatically scheduled. Do not inflate packet adequacy into target competence.
